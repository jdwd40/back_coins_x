# AUTOBUILD STATE

## Current Execution

- **Current stage:** Stage 8 COMPLETE (persistent bots + bot-only debt); Stage 9 next (Stage 7 persistent path is satisfied by construction — no Power/position-cap/round inputs exist in the persistent economy or its new frontend; old-surface retirement stays Stage 13 debt until the old deployed frontend is replaced)
- **Current work unit:** S8-01/S8-02 done; focused persistent regression green (8 suites, 130/130: persistent-api, persistent-economy-db, persistent-debt-db, persistent-bots-db, persistent-world-db, market-persistent-writer, persistent-signals, users); schema verifier PASS through 028; frontend gates green (unit 241/241, UI contract, lint 0/6, build); `git diff --check` clean in both repos
- **Status:** CHECKPOINT — uncommitted working-tree checkpoint (no commits/pushes permitted or made)
- **Current branch:** Backend `persistent-market-autobuild-20260831`; frontend `persistent-market-autobuild-20260831` (both from remote `security-preflight-persistent-market`)
- **Latest successful commit:** None this run (no commits permitted)
- **Last pushed commit:** None this run
- **Last deployed commit:** Not checked (deploy forbidden this run)
- **Database migration status:** Migrations 023–028 applied to disposable `coins_test`; schema verification passes (verifier extended for 027 debt/loans and 028 bot ticks). Next migration number: 029. Production not checked or applied
- **Review status:** Self-reviewed diffs; independent controller review pending
- **Blocking issue:** None
- **Next action:** Stage 9 S9-01 (explicit permanent coin death decision rule + authored replacement pool with configurable delay, explicit archetypes [missing archetype fails validation, never default MOON], ~10 active coins, roster churn simulation with survivors/recoveries/occasional deaths, no extinction cascade). Remaining: Stage 10 persistent leaderboard, Stage 11 frontend experience, Stage 12 diagnostics, Stage 13 cleanup, Stage 14 gates+report

## Startup Safety Check

- Backend HEAD `6b0a9b4`; frontend HEAD `1bb4454`. Backend pre-existing untracked `.hermes/` preserved, never staged. Frontend worktree untouched.
- Disposable test DB guard asserted in every mutating suite; all DB tests run `NODE_ENV=test` on `coins_test`. Only one DB-mutating Jest process runs at a time; no source/test file edits during a running full regression (a mid-run edit to verify-game-schema.js polluted one regression attempt — rerun clean).
- No SSH/production/PM2/deploy/workflow actions taken or planned. No commits/pushes made.

## Tests

- **Baseline backend:** sole pre-existing failure is the 5s timeout in `__tests__/game-public-state-no-seed.test.js` (unchanged).
- **Repair (this pass, first priority):** `persistent-world-db.test.js` migration-024 existing-database test failed after Stage 3 landed (`missing FOREIGN KEY on market_director_state -> market_worlds (world_id)`). Root cause: `DROP TABLE market_worlds CASCADE` drops the DEPENDENT CONSTRAINTS that reference it but leaves the dependent tables standing FK-less; replaying a dependent migration alone hits its shape-no-op path which never recreates the FK. Honest fixture repair: drop the whole dependent subtree explicitly, remove ALL affected ledger records (ledger must match physical reality), replay the migrations in order, assert the restored dependents; verifier and FKs untouched. After Stage 5's migration 026 the same fixture needed the same one-step-deeper chain (024+025+026) — fixed identically and documented in the test ("any future world-dependent migration must extend this replay chain"). Focused suite 13/13 green.
- **Stage 4 focused suite (all passing, 19 tests):** `market-persistent-writer.test.js` — world resolved once/batch, no-world abort, Director environment threaded to every coin, zero Apocalypse cycle reconciliation/creation, pinned-instant determinism, no in-memory pricing state, invalid-price full rollback, corrupt-future-checkpoint full rollback (corrupt row preserved), off-roster coin abort (never defaults), retired off-roster coin skipped as preserved history, atomic price+history+coin-state+checkpoint+Director-cursor commit bit-identical to the origin walk, second-batch resume from persisted checkpoints + committed Director cursor (resumeDirectorCursor verified, tampered cursor aborts loudly), persistent death (never priced/revived, zero-price guard, malformed-state abort, replay-idempotent), provenance (cycle_id NULL / MARKET_TICK / batch-instant created_at), redaction (no Director internals/seeds in public status/stats).
- **Stage 5 focused suite (all passing, 26 tests):** `persistent-economy-db.test.js` — migration 026 apply/preserve/no-op + verifier; exactly-once £10,000 (replay no-op, no re-grant after spending, concurrent first-provisioning race grants once, UNIQUE backstop, no-world loud failure); BUY (exact debit, cost basis, ledger-after-success, overspend rejection with zero trace, minimum notional, quantity validation, DEAD-coin and retired-coin rejection, concurrent-buy race can never overspend, weighted-average basis); SELL (exact credit, proportionate basis, full-sale zeroes basis, oversell rejection with zero trace, no-holding rejection, DEAD-coin rejection, concurrent-sell race can never oversell); account state reads (unprovisioned null, wealth = cash + live holdings value).
- **Stage 6 backend focused suite (all passing, 12 tests):** `persistent-api.test.js` — authenticated persistent buy/sell at the server-locked price over HTTP, exactly-once provisioning via the API, 401 unauthenticated writes nothing, body user_id cannot steer another account (token is the owner), 400/404 domain errors (never 500), account read provisioned:false → live state, payload redaction (no seed/regime internals), registration provisions exactly one £10,000 persistent account idempotently, **server-owned holding economics (averageEntryPrice/currentValue/unrealizedPnl/unrealizedPnlPct recomputed at read time)**, **bounded transaction history (GET /api/persistent/transactions): own ledger only, newest first, ?limit validated loudly (400) and capped at 100, unprovisioned reads an empty history, redaction**.
- **Stage 6 frontend (this pass, all gates green):** new gameplay flows (buy/sell/account/portfolio/transactions) run ONLY on `/api/persistent/*`: `src/services/persistentService.ts` (typed client; requests exactly `{ coin_id, quantity }` — no price, no user id, NO cycle/Apocalypse identifier; strict boundary parsers with `forbidCycleFields` loud rejection; 401 → SessionExpiredError; `{status:'success',data}` envelope); `src/context/PersistentContext.tsx` (5s shared authenticated account poll, post-trade adoption of the server-returned account, identity-change reset, never-fabricate states); `src/components/PersistentTradePanel.tsx` (quantity entry vs displayed price, confirm flow, server-locked-price copy, dead-coin permanent-death block BOTH directions, auth/sync/unavailable gates, verbatim domain rejections); `src/utils/persistentTrading.ts` (pure gate helpers). Consumers rewired: CoinSignalCard (quick buys + sells + persistent holding economics; Power ⚡ UI and position-cap gates removed), GameCoinDetail (persistent panel; no Power line; chart never clipped to a cycle), CoinDetail (persistent panel), GameMarketGrid (ownership from the persistent account), PlayerStatusStrip (persistent Cash/Wealth/holdings/positions — Power and /3 cap cells removed), PlayerRoundPanel (persistent account + own-trade activity feed), Profile (persistent portfolio/transactions; legacy funds removed), App (PersistentProvider mounted). RoundTradePanel/BuyForm/SellForm/transactionService/gameService/GameContext RETAINED unchanged for the old-frontend compatibility surfaces (leaderboard/results/header still cycle-shaped — Stage 10/11 work). New focused tests: `persistentService.test.ts` 15 (exact request shapes, no-cycle loud rejection, provisioned:false, error envelopes), `persistentTrading.test.ts` 6. UI contract extended: persistent section pins endpoints/request shapes/states/no-cycle; retired Power/dead-sell/round assertions replaced at equal rigor. Gates: unit 241/241 (baseline 220 + 21 new), UI contract PASS, lint 0 errors/6 warnings, build PASS.
- **Regression spot checks (all green):** writer-migration suites (9 suites / 88 tests), writer-adjacent + persistent Stage 2/3 suites (12 suites), `users.test.js` 22/22 with registration provisioning, `persistent-economy-db` 26/26, `persistent-api` 8/8, `verify:game-schema` PASS (026 included).
- **Full backend regression (final this pass, clean serial run, no concurrent edits):** 106/107 suites, 1129/1130 tests, 885.684s — sole failure is the known pre-existing 5s timeout in `game-public-state-no-seed.test.js` (unchanged baseline). The repair target `persistent-world-db.test.js` is green inside the full suite; `verify:game-schema` PASS after; `git diff --check` clean; no workflow/env/package-lock paths touched. Controller should still independently rerun `NODE_ENV=test npx jest --runInBand`.
- **Stage 2/3 gates (prior, controller-verified):** focused 8 suites / 93 tests; neutral 365-day horizon (17,520 steps, replay bit-identity 1,850 samples); Director 90-day horizon (4,320 steps, replay 450 samples); schema verifier PASS (023+024+025).
- **Legacy `npm run simulate -- --mode multi-cycle`:** controller-reported 420s timeout pre-continuation; still pending bounded/background rerun (multi-cycle JEST suite green).

## Completed Work Units (this continuation)

- S2-03 Persistent pricing engine: `game/persistentPricing.js` — pure deterministic composition over the Stage 1 machinery. §22 log-space structural drift (symmetric log draw, zero expected log drift); §23 weak log-space restoring force toward the structural reference (0.005/cycle, measured-stable); §24/§25 decaying crash damage in FACTOR space (pow/multiply leaky integrator, 2-day half-life; activation committed under the environment at episode start — no retroactive rewrite; factor-space math keeps freeze/resume bit-identical to the origin walk with no log/exp round-trip); §26 decaying peak (30-day half-life); §27 living floor only. Checkpoint contract composes migration 023's table with `activationContext: 'PERSISTENT'` (never invalidates on regime change); loud §18 validation; in-flight episode rule preserved; explicit archetypes only (§29).
- S2-03 config: `simulationConfig.js` gains a validated `persistent` section (seventh top-level section) with measured neutral balance (365-day probed: per-coin bands e.g. ZIP 0.30-2.80×, RUG 0.07-8.12× reference; no secular collapse, no floor pinning).
- S2-04a Environment seam: `game/marketEnvironment.js` — one coherent Market Environment (plan §9 fields), loud bounds validation, deterministic NEUTRAL provider; `resolveEnvironment` accepts a literal or any provider (the Stage 3 Director plugs in behind this seam without pricing changes).
- S2-04b Public layers: `collapseRiskDomain.getPersistentCollapseRisk` (condition-driven danger replaces apocalypsePercent; public inputs + schedule-independent noise only) and `game/persistentSignals.js` (the persistent public coin signal — the shape the human endpoint and bot layer will share), `deadPersistentSignal`, exact public key allowlist.
- S2-04 fairness: `__tests__/persistent-signals.test.js` — humans/bots share one signal; poisoned observations fail `botService.assertPublicBotState` (forbidden:seed/damageFactor/nextRegimeRoll/structuralReference/directorRolls); no hidden rolls/probabilities reach a bot.
- **Plan gap (Stage 1 §20) RESOLVED:** `simulation/roundEnvironment.js` now prices through the SAME checkpointed production path (`game/pricingCheckpoint.js` over `game/priceEngine.js`): per-coin monotone accumulator cache resumes/freezes like the live writer, lookbacks price from the origin (identical stateless engine), cache never rewinds. Automated parity: `checkpointed(originState) === existingEngine(originState)` bit-for-bit over dense grids, lookback order, and lifecycle transitions; load-bearing resume/extract proven by spy counts and accumulator depth.
- S2-05 `simulation/persistentHorizon.js` (+ `npm run simulate:persistent-horizon`): full committed-state world stepping (condition/reference/peak per batch, checkpoint resume/freeze), hard invariants + 30-day-window variety + replay identity + per-coin balance distributions.
- S3-01 Migration 025 `market_director_state` (one authoritative Director cursor per world: regime/regime_started_at/intensity/regime_index; CHECK vocabulary + bounds; FK to market_worlds) wired into `db/seed.js` + `db/verify-game-schema.js` (`verifyMarketDirectorState`) + `models/marketDirectorState.model.js` (validated, replay-safe upsert; corrupt state fails loudly at model AND SQL layers).
- S3-02 `game/marketDirector.js`: deterministic six-regime Director behind the environment seam — seeded chain (own domain separator), bounded durations/intensities, seeded transition matrix with no-absorbing/believable-hope validation, intensity-lerped environments through the seam, monotone memoised cursor (lookbacks re-walk from origin), publicRegimeAt (regime + intensity ONLY, no rolls/chain internals). Chain walk cursor-resume identity proven; pre-epoch instants resolve to genesis (staggered-start convention).
- S3-02 config: validated `director` section (eighth section) — six regime templates (duration/intensity ranges, full-intensity environments with sign contract), initialWeights + transitionWeights (normalised, no absorbing, believable hope enforced).
- Stage 3 simulation threading: `simulate:persistent-horizon --provider director` (Director provider behind the same seam; neutral remains default).
- **Repair (this pass):** `__tests__/persistent-world-db.test.js` migration-024 fixture — drop the full dependent subtree (024+025 tables), remove both ledger records, replay both migrations; verifier/025 FK intact (see Tests).
- **S4-01 Runtime authority (this pass):** `models/market-simulator.js` rewritten persistent-authoritative. Each batch: resolve THE active world once (fail loudly when none — never fabricated); BEGIN; lock coins FOR UPDATE (coins-first lock order preserved); load per-coin market state, committed Director cursor and world-seed pricing checkpoints (all FOR UPDATE); build the Director provider resumed from the committed cursor (NEW `marketDirector.resumeDirectorCursor` — rebuilds the walk cursor from the committed row, re-deriving and BIT-verifying duration/intensity from the pure chain, loud on tamper; provider accepts an initial cursor); per coin — open missing state explicitly (roster archetype only, opening reference = live price else restoration baseline, else abort; RETIRED off-roster coins skipped as preserved history), persistent-DEAD guard (stays £0, never priced, malformed non-zero aborts), price via `persistentPricing.persistentPriceAt` with checkpoint resume, advance condition (recent log return reconstructed from PERSISTED ticks — database state, restart-exact)/reference/peak with elapsed = batch instant − committed checkpoint instant, write coins + price_history (cycle_id NULL, batch-instant created_at, MARKET_TICK) + fresh checkpoint + coin state; commit the Director cursor (own table, same transaction); market_history trend from PERSISTED tick lookback (index-backed, no origin re-walk); old-economy `reconcileActivePeaks` retained (compatibility, no new rounds/cycles); COMMIT. `updateAllPrices({ nowMs })` accepts injected authoritative time for simulated-time/replay tests. ZERO imports of gameCycleService/apocalypseVolatility/dynamicCollapseService/pricingContext/priceEngine remain in the writer. No hidden Director data to bots: the writer's public surface carries no regime internals; only regime+intensity is public anywhere (allowlist/redaction suites unchanged and green).
- **Writer-contract migration (Stage 4, deliberate contract change — coverage replaced at equal rigor, NOT weakened):** removed `v2-market-writer.test.js`, `unified-price-path.test.js`, `market-collapse.test.js` (every test asserted the retired V2 writer contract; dynamic-collapse ENGINE coverage remains in `dynamic-collapse.test.js` (28), `collapse-race`, `transactions-collapse`, `migration-dynamic-collapse`, `verify-schema-collapse-zero`, `v2-collapse-risk`). `market-apocalypse-volatility.test.js` now pins the legacy domain engine only. `pricing-checkpoint-db.test.js` keeps migration-023/round-trip/resume coverage; its V2 live-writer describe moved to the persistent suite. `price-history-provenance.test.js` keeps collapse-writer + legacy-NULL coverage; tick provenance coverage moved. `game-round-lifecycle.test.js` peak test and `market-simulator.test.js` now provision the world explicitly before batches.
- **S5-01 Persistent economy (this pass):** migration 026 (`persistent_accounts` UNIQUE (world_id,user_id) = the exactly-once £10,000 receipt; `persistent_holdings` UNIQUE (account_id,coin_id) with weighted-average DECIMAL cost basis; `persistent_transactions` append-only BUY/SELL ledger) + verifier checks + seed wiring. NEW `game/persistentEconomy.js` adapts the proven Apocalypse trade transaction: one client/one transaction, server-locked price (never client input), coins-before-account lock order, guarded SQL debit (`cash >= $1`) / decrement (`quantity >= $1`) as the race backstop, validation + £0.01 minimum notional, ledger-after-success, full rollback on any failure. Persistent death stops trading BOTH directions (Stage 9 contract); retired coins reject buys; a coin with no state row yet trades at the live price. No Power/position-cap/round inputs anywhere in the persistent economy.
- **S6-backend Persistent API (this pass):** NEW `routes/persistent.routes.js` mounted at `/api/persistent` (additive; old cycle-shaped routes untouched) + `controllers/persistent.controller.js` (POST /trades/buy|sell, GET /account; token is the account owner — body user_id ignored; domain errors carry 400/404; payloads carry no seed/Director internals). Registration (`controllers/users.controller.js`) now ALSO provisions the persistent account best-effort and idempotently (no round enrolment requirement; the first persistent trade is the idempotent safety net before world provisioning at deployment).

## Stage 2 Balance (measured, not gated — §53)

- Neutral 90-day: references evolve BOTH directions (e.g. FTR 0.10→0.21 peak, BLN 43→200 peak, NVC ~1.27-2.10 stable) — the earlier condition→reference downward spiral was fixed by centering the drawdown/damage terms on measured per-archetype neutral typicals (`typicalDrawdown`, `typicalLogCommittedDamage` config maps).
- Conditions range widely (e.g. −0.94..+0.88) without pinning; damage factor bands ~[0.25, 2.05]; no coin pinned to the living floor.
- Committed damage means are near zero per archetype (rallies slightly overshoot); log returns mean ~0 (log-neutrality measured: mean 6h log return −0.0011..+0.0007 across archetypes).
- **Invariant correction (365-day first run):** the original harness invariant compared price to the OPENING reference (max 100x); a legitimately thriving coin's reference evolves, so price/opening-reference can exceed 100x without numerical runaway. Invariants now check (a) price vs the CURRENT structural reference it was priced against ([0.005, 200] band — decoupling = numerical runaway/extinction), (b) reference evolution sanity ([0.001, 1000]x opening reference over the horizon, (c) floor pinning forbidden, condition/reference bounds. This corrects a same-day mis-designed invariant to test what the plan requires (§53 hard failures: non-finite price, runaway overflow, market extinction — not balance judgement).

## Compatibility Shims

- `PATCH /api/users/:user_id/funds` — retained route returning uniform 403. Reason: previously deployed frontend may still call it; safe denial beats 404. Consumer: old deployed frontend. Removal event: post-deploy frontend retirement of legacy funds UI (Stage 6+ debt).
- Old cycle-shaped API surface (`/api/game/*`, `/api/transactions/*`) — fully retained and unchanged while the old deployed frontend exists. The persistent surface (`/api/persistent/*`) is additive. Reason: Stage 6 compatibility contract — old requests must keep working safely until the persistent frontend deploys. Consumer: old deployed frontend. Removal event: post-deploy cutover + proven-unreachable analysis (Stage 13 debt).
- Old runtime workers (gameCycleWorker/economyWorker/botWorker) still START and reconcile legacy cycles for the old read surface; they no longer drive prices (the writer is persistent-authoritative). Removal event: Stage 13 after consumers migrate and unreachability is proven.
- `gameRoundService.reconcileActivePeaks` inside the writer batch — legacy peak_wealth keeps tracking live prices for the old round surface. Removal event: Stage 7/13 with the old round surface.

## Design Decisions (this continuation)

- The persistent engine carries its own cycle walk (`game/persistentPricing.js`) composing marketDomain's cycle shape stream + priceEngine's episode draws, with its own log-drift/noise streams (own domain separators): the V2 engine is untouched for legacy/parity; the persistent path never takes apocalypsePercent/lifecycle/amplitude inputs.
- Damage accumulator lives in FACTOR space (pow/multiply) precisely so the frozen accumulator is the exact double the walk holds — bit-identity without log/exp round-trips.
- Condition dynamics measure EXCESS over per-archetype measured neutral typicals (config `persistent.condition.typicalDrawdown/typicalLogCommittedDamage`): condition near zero mean in neutral → no secular reference drift. The typicals are measured engine properties, documented in config; Stage 3+ re-measurement is a Director-era balance task.
- roundEnvironment checkpointing: per-coin monotone cache — resume when legal, origin (identical stateless engine) for lookbacks behind the frozen checkpoint; the cache never rewinds; activation-context invalidation preserved.
- Persistent world simulation threads coin events as zero modifier for Stage 2 (the V2 coin-event engine is cycle-scoped; a persistent event stream is Stage 3/4 work).

## Known Non-Blocking Issues

- Pre-existing baseline failure: `game-public-state-no-seed.test.js` settled-cycles 5s timeout (unchanged).
- Stage 8: the persistent bot worker starts only under NODE_ENV=production (same lifecycle contract as the legacy workers) and loud-skips ticks until a world is provisioned at deployment. The legacy per-bot cooldown/last_action_at discipline was NOT ported: the deterministic per-tick decision + DB-claimed tick identity is the cadence control; if sub-tick cooldowns are wanted, that is a tuning follow-up, not an invariant gap.
- Stage 6 frontend: ApocalypseHeader/LeaderboardPanel/LeaderboardPressure/ResultsPanel/HowToPlay still read the cycle-shaped GameContext (retained compatibility surfaces — Stage 10/11 replace them; GameContext itself is untouched). RoundTradePanel/BuyForm/SellForm/transactionService remain mounted nowhere but are retained per the no-premature-deletion rule.
- Legacy `npm run simulate -- --mode multi-cycle` exceeded the controller's 420s foreground timeout pre-continuation; rerun bounded/background still pending (multi-cycle JEST suite 26/26 green).
- Persistent coin-event stream not yet threaded (writer and Stage 2 harness use netEventModifier 0); persistent event stream is recorded debt for a later stage (Stage 6 API era at the latest).
- Persistent trading-pressure modifier not yet threaded (pressureModifier 0): it needs the Stage 5 persistent ledger as its source; recorded debt for Stage 5+.
- The old runtime path (gameCycleWorker/economyWorker/botWorker, cycle reads) remains STARTED for old-API compatibility until consumers migrate (Stages 6–8); it is legacy-only and no longer drives prices. Its retirement is Stage 13 cleanup after proven unreachable.
- World provisioning at deployment is an explicit operational step (the writer never fabricates a world); recorded for the Stage 14 release-candidate report.

## Changed Files This Run (cumulative uncommitted checkpoint)

- Modified (Stages 0-1, prior run): `controllers/users.controller.js`, `__tests__/user-funds-security.test.js`, `__tests__/user-funds.test.js`, `game/marketDomain.js`, `game/priceEngine.js`, `models/market-simulator.js`, `db/seed.js`, `db/verify-game-schema.js`, `package.json`
- New (Stages 0-2 schema/state, prior run): `db/migrations/023_create_market_price_checkpoints.sql`, `db/migrations/024_create_persistent_world.sql`, `game/pricingCheckpoint.js`, `game/persistentWorld.js`, `models/pricingCheckpoint.model.js`, `models/marketCoinState.model.js`, `simulation/checkpointHorizon.js`, `__tests__/pricing-checkpoint.test.js`, `__tests__/pricing-checkpoint-db.test.js`, `__tests__/persistent-world-db.test.js`
- Modified (Stage 2-3 continuation): `game/simulationConfig.js`, `game/collapseRiskDomain.js`, `simulation/roundEnvironment.js`, `package.json`, `__tests__/simulation-config.test.js`, `AUTOBUILD_STATE.md`
- New (Stage 2-3 continuation): `game/marketEnvironment.js`, `game/persistentPricing.js`, `game/persistentSignals.js`, `simulation/persistentHorizon.js`, `__tests__/market-environment.test.js`, `__tests__/persistent-pricing.test.js`, `__tests__/persistent-signals.test.js`, `__tests__/persistent-horizon.test.js`, `__tests__/round-environment-checkpoint-parity.test.js`, `db/migrations/025_create_market_director_state.sql`, `game/marketDirector.js`, `models/marketDirectorState.model.js`, `__tests__/market-director.test.js`, `__tests__/market-director-db.test.js`
- Modified (this pass — Stage 4 S4-01): `models/market-simulator.js` (persistent-authoritative writer), `game/marketDirector.js` (resumeDirectorCursor + provider initial cursor), `__tests__/persistent-world-db.test.js` (migration fixture repair), `__tests__/market-apocalypse-volatility.test.js` (legacy-domain-only coverage), `__tests__/pricing-checkpoint-db.test.js` (V2-writer describe removed), `__tests__/price-history-provenance.test.js` (writer-tick describe removed), `__tests__/game-round-lifecycle.test.js` (world provisioning in peak test), `__tests__/market-simulator.test.js` (world provisioning), `AUTOBUILD_STATE.md`
- New (this pass — Stage 4): `__tests__/market-persistent-writer.test.js` (19 tests — the Stage 4 writer contract)
- Removed (this pass — retired V2 writer contract, coverage replaced at equal rigor in `market-persistent-writer.test.js`; legacy engine coverage retained elsewhere): `__tests__/v2-market-writer.test.js`, `__tests__/unified-price-path.test.js`, `__tests__/market-collapse.test.js`
- New (this pass — Stage 5): `db/migrations/026_create_persistent_economy.sql`, `game/persistentEconomy.js`, `__tests__/persistent-economy-db.test.js` (26 tests)
- Modified (this pass — Stage 5): `db/seed.js` (026 DDL + reseed drops), `db/verify-game-schema.js` (verifyPersistentEconomy), `__tests__/persistent-world-db.test.js` (replay chain extended to 026)
- New (this pass — Stage 6 backend): `routes/persistent.routes.js`, `controllers/persistent.controller.js`, `__tests__/persistent-api.test.js` (8 tests)
- Modified (this pass — Stage 6 backend): `app.js` (mount /api/persistent), `controllers/users.controller.js` (idempotent persistent provisioning at registration)
- Modified (this pass — Stage 6 frontend continuation, backend): `game/persistentEconomy.js` (getPersistentTransactions bounded history + validateTransactionLimit; holdings carry averageEntryPrice/unrealizedPnl/unrealizedPnlPct), `controllers/persistent.controller.js` (GET /transactions), `routes/persistent.routes.js` (mount), `__tests__/persistent-api.test.js` (12 tests — +transactions history, +holding economics), `AUTOBUILD_STATE.md`
- New (this pass — Stage 6 frontend, fcoins_y): `src/services/persistentService.ts`, `src/services/persistentService.test.ts` (15), `src/context/PersistentContext.tsx`, `src/components/PersistentTradePanel.tsx`, `src/utils/persistentTrading.ts`, `src/utils/persistentTrading.test.ts` (6)
- Modified (this pass — Stage 6 frontend, fcoins_y): `src/App.tsx` (PersistentProvider; PlayerRoundPanel props), `src/components/CoinSignalCard.tsx` (persistent trades/holdings; Power UI removed), `src/components/GameCoinDetail.tsx`, `src/components/CoinDetail.tsx`, `src/components/GameMarketGrid.tsx`, `src/components/PlayerStatusStrip.tsx`, `src/components/PlayerRoundPanel.tsx`, `src/components/Profile.tsx`, `scripts/ui-contract.mjs` (persistent section; retired-surface assertions replaced at equal rigor), `package.json` (test:unit gains the two new suites)
- **Stage 8 S8-01/S8-02 (this pass):** migration 027 (`persistent_accounts.debt` DECIMAL CHECK >= 0 + append-only `persistent_loans` ledger with debt_after) and migration 028 (`persistent_bot_ticks` world-scoped tick identity, PK (world_id, tick_id)); verifier + seed wiring for both; `game/gameConstants.js` gains PERSISTENT_BOT_LOAN_AMOUNT/PERSISTENT_BOT_OPERATING_RESERVE with validated env resolvers; NEW `game/persistentDebt.js` (pure bankruptcy predicate — no usable cash AND no meaningful sellable holdings, dead holdings are £0 unsellable history; bot-only exactly-£10,000 interest-free loans, each requiring fresh bankruptcy, concurrent races issue once via row lock; automatic repayment of min(cash-reserve, debt) with the guarded debit as backstop; debt-adjusted `netWealth` in the account read; bounded loan-ledger read); NEW `game/persistentBots.js` (shaped public-state allowlist `assertPublicPersistentBotState` — no seed/Director/cycle/Power keys; deterministic decision layer over the SAME BOT_PERSONALITY_PROFILES: panic/profit-take/loss-cut/risk-exit exits, phase/momentum/archetype/contrarian entries bounded by stake fraction, per-trade cap, invested-fraction cap and cash reserve; bankruptcy → LOAN decision; tick execution through the shared persistentEconomy/persistentDebt services with non-fatal rejection skips); NEW `game/persistentBotWorker.js` (lifecycle-owned, DB-claimed ticks, no world fabrication) started/stopped in server.js. Account state contract gains `debt` + `netWealth` (frontend parser/panels updated; wealth displays show netWealth). Replays chains in persistent-world-db/persistent-economy-db fixtures extended to 027/028 per the documented rule. Focused suites: persistent-debt-db 14/14, persistent-bots-db 14/14 (incl. 120-tick bounded long-run invariants: no negative cash/debt, ledger reconstructs debt exactly, loans are the only money source beyond grants and bounded by tick count).
