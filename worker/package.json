{
  "name": "coins-market-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Coins market simulation worker — calls coins.run_market_tick via the restricted coins_worker role only. Never uses the Supabase service-role key.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
