name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: 213.165.91.221
          port: 4020
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/jd/back_coinsx
            git pull origin main
            npm install
            cd frontend
            npm install
            npm run build
            cd ..
            pm2 restart all
